<UserControl x:Class="TRIBOLOGY.ViewParams"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <ScrollViewer HorizontalScrollBarVisibility="Auto" >
        <Grid Style="{StaticResource ContentRoot}">
            <mui:BBCodeBlock x:Name="TextBlock" Margin="100,0,0,47" FontSize="16" />
            <Button x:Name="SaveAsBtn" Content="另存为" HorizontalAlignment="Right" Height="32" VerticalAlignment="Bottom" Width="99" FontSize="16" Click="SaveAsBtn_Click" Margin="0,0,0,10"/>
        </Grid>
    </ScrollViewer>
</UserControl>
